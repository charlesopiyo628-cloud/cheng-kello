[phases.setup]
cmds = ["composer install --no-interaction --no-progress --optimize-autoloader"]

[phases.build]
cmds = ["php artisan config:cache", "php artisan route:cache"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT}"