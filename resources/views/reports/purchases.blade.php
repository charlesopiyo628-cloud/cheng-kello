<!DOCTYPE html>
<html>
<head>
    <title>CHENG KELO FRESH FISH - Purchase Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { text-align: center; border-bottom: 2px solid #333; padding-bottom: 20px; margin-bottom: 30px; }
        .stats-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-bottom: 30px; }
        .stat-box { border: 1px solid #ddd; padding: 15px; text-align: center; }
        .stat-number { font-size: 24px; font-weight: bold; color: #2c3e50; }
        table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        th { background-color: #f2f2f2; }
        .footer { margin-top: 50px; text-align: center; font-size: 12px; color: #666; }
        @media print { body { margin: 10px; } }
    </style>
</head>
<body>
    <div class="header">
        <h1>CHENG KELO FRESH FISH</h1>
        <h2>Purchase Report</h2>
        <p>Generated on: {{ $data['report_date'] }}</p>
        <p>Generated by: {{ $data['generated_by'] }}</p>
    </div>

    <div class="stats-grid">
        <div class="stat-box">
            <div class="stat-number">{{ $data['total_purchases'] }}</div>
            <div>Total Quantity Purchased</div>
        </div>
        <div class="stat-box">
            <div class="stat-number">UGX {{ number_format($data['total_cost'], 2) }}</div>
            <div>Total Purchase Cost</div>
        </div>
        <div class="stat-box">
            <div class="stat-number">UGX {{ number_format($data['today_purchases'], 2) }}</div>
            <div>Today's Purchases</div>
        </div>
    </div>

    <h2>Purchase Details</h2>
    <table>
        <thead>
            <tr>
                <th>Date</th>
                <th>Category</th>
                <th>Quantity</th>
                <th>Cost Price</th>
                <th>Total Cost</th>
                <th>Supplier</th>
                <th>Invoice</th>
                <th>Created By</th>
            </tr>
        </thead>
        <tbody>
            @foreach($data['purchases'] as $purchase)
            <tr>
                <td>{{ $purchase->purchase_date->format('d/m/Y') }}</td>
                <td>{{ $purchase->fishCategory->name }}</td>
                <td>{{ $purchase->quantity }}</td>
                <td>UGX {{ number_format($purchase->cost_price, 2) }}</td>
                <td>UGX {{ number_format($purchase->total_cost, 2) }}</td>
                <td>{{ $purchase->supplier_name ?? 'N/A' }}</td>
                <td>{{ $purchase->invoice_number ?? 'N/A' }}</td>
                <td>{{ $purchase->createdBy->name }}</td>
            </tr>
            @endforeach
        </tbody>
    </table>

    <div class="footer">
        <p>Generated by CHENG KELO FRESH FISH - {{ now()->format('Y') }}</p>
    </div>
</body>
</html>
